/**
 * Weak matches are only promoted to a comparable deal when score ≥ this threshold.
 * Tuned so borderline listings stay out; adjust with real traffic.
 */
export const WEAK_MATCH_MIN_SCORE = 44;
